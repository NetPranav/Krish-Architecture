# ╔══════════════════════════════════════════════════════╗
# ║  SmartAgri · Backend Dependencies                    ║
# ║  Python 3.10+ · Render-Deployable                    ║
# ╚══════════════════════════════════════════════════════╝

# Core ML (CPU-only, no GPU required on server)
xgboost>=2.0.0
scikit-learn>=1.4.0
numpy>=1.26.0
pandas>=2.2.0

# Hyperparameter Optimisation (Bayesian)
optuna>=3.6.0

# Model serialization
joblib>=1.4.0

# Utilities
tqdm>=4.66.0

# ── API Server ────────────────────────────────────────
httpx>=0.27.0               # HTTP client for Roboflow API
fastapi>=0.115.0            # API framework
uvicorn[standard]>=0.30.0   # ASGI server for FastAPI
python-multipart>=0.0.9     # Required for FastAPI file uploads
python-dotenv>=1.0.0        # Loads .env for API keys

# ── NVIDIA NIM LLM API ───────────────────────────────
openai>=1.30.0              # OpenAI-compatible SDK for NVIDIA NIM API

# ── Deployment Health Check ───────────────────────────
gunicorn>=22.0.0            # Production WSGI/ASGI server for Render
